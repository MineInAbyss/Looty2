package com.derongan.minecraft.looty.skill.component;

import com.derongan.minecraft.looty.skill.component.proto.GroundedInfo;

/**
 * Forces reference points to be on the ground
 */
public class Grounded extends Component<GroundedInfo> {
    public Grounded(GroundedInfo info) {
        super(info);
    }
}
